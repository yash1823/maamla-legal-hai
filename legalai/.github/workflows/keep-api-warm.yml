name: Keep FastAPI Backend Warm

on:
  schedule:
    - cron: "*/7 * * * *" 
  workflow_dispatch:

jobs:
  ping-api:
    runs-on: ubuntu-latest
    steps:
      - name: Ping FastAPI health route
        run: |
          curl "https://maamla-legal-hai-api.onrender.com/health?token=${{ secrets.HEALTH_CHECK_TOKEN }}"
